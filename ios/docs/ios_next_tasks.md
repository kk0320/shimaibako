# iOS次タスク

## 優先度高

1. 実機画面で写真権限フローを確認する
2. 限定アクセスで選択写真だけが表示されることを確認する
3. 3万枚規模の実機ライブラリで軽量/標準/多め/大量モードの表示負荷を確認する
4. 大量/フルモードの安全確認画面を実機で確認する
5. 実機でOCR結果の保存と再起動後の復元を確認する
6. 実機で検索インデックス件数、OCR件数、分類済み件数が再起動後に復元されることを確認する
7. 実機でOCRテキスト検索とカテゴリ別件数の表示速度を確認する
8. 実機でまとめてOCR中のキャンセル、連続タップ、バックグラウンド移行時の挙動を確認する
9. iCloud取得を許可した場合とオフライン優先の場合のOCR失敗表示を確認する
10. 実機で1枚OCR結果削除、表示中OCR結果削除、全OCR結果削除後の復元状態を確認する
11. 実機で分類の未分類戻し、再判定、分類再構築を確認する
12. 実機のスクショで、地図・予約・メモ・買い物などの細分類が候補表示されることを確認する
13. 実機の一般写真で、追加カテゴリが過剰に断定表示されないことを確認する
14. 実機で分類手動変更後に、分類傾向学習データ件数が増えることを確認する
15. 実機で分類傾向学習オン/オフと学習データ削除を確認する
16. 実機で学習データ削除後も写真本体、OCR結果、手動分類が残ることを確認する
17. 実機で学習由来候補が過剰に分類を変えないことを確認する
18. 実機で精度向上モードのオン/オフ、実行タイミング、iCloud取得設定が保存されることを確認する
19. 実機で「今すぐ少しだけ実行」を開始し、最大50件で完了または中断することを確認する
20. 実機で精度向上モード実行中のキャンセルと中断理由表示を確認する
21. 実機で低電力モード、発熱、空き容量不足などの安全条件表示を確認する
22. 実機で精度向上データ削除後も写真本体、OCR結果、手動分類が残ることを確認する

## 優先度中

1. SQLiteの検索結果をDBカーソルで200件ずつ返す
2. カテゴリ別件数、表示状態件数をDB集計へさらに寄せる
3. SQLite FTSによるOCRテキスト検索を試作する
4. OCRジョブを `processing_jobs` / `processing_job_items` に永続化する
5. UIテストターゲットを追加する
6. 写真グリッドの空状態と読み込み状態を追加検証する
7. スクリーンショット判定の精度を上げる
8. 動画サムネイル表示と種別バッジを追加確認する
9. SettingsViewにバージョン情報を追加する
10. OCR結果削除と分類再構築のUIテストを追加する
11. カテゴリ推定の辞書を実機写真に合わせて調整する
12. 写真詳細画面のメタデータ表示を増やす
13. スクショ細分類のキーワード辞書を実機データに合わせて調整する
14. 花・植物、建物、芸術品、食べ物、動物などは端末内の画像特徴量ベース分類を検討する
15. 分類傾向学習例の一覧確認UIを検討する
16. 学習由来候補の誤分類を弱めるための除外キーワードを調整する
17. 精度向上モードの実機結果を見て、BGTaskScheduler/BGProcessingTaskによる夜間試行を設計する
18. 精度向上モードの処理履歴をより詳しく見られる画面を検討する

## 優先度低

1. `OCRResultStore` をSQLiteへ統合する
2. 分類傾向学習データをSQLiteへ移行する
3. `PHPhotoLibraryChangeObserver` による差分反映を追加する
4. 追加ファイル種別への拡張方針を決める
5. OCR認識言語と精度をユーザーが変更できる形にする
6. OCR処理履歴を見られるようにする

## 検証コマンド

```sh
cd ~/Desktop/all_dev/shimaibako
xcodebuild build \
  -project ios/ShimaiBako/ShimaiBako.xcodeproj \
  -scheme ShimaiBako \
  -destination 'generic/platform=iOS Simulator' \
  -derivedDataPath /tmp/shimaibako-final-validate

swift ios/scripts/index_store_performance_check.swift
swift ios/scripts/photo_library_safety_check.swift
```

## 注意点

- 写真は外部送信しない
- 写真の削除、移動、リネーム、上書き機能は作らない
- 写真アプリ側にアルバムやフォルダを作らない
- 3万枚規模では初回から全件OCRを行わない
- まずメタデータと軽量分類を使い、OCRは表示中、スクショ、書類候補などに絞って段階実行する
- 検索インデックスには写真本体やサムネイル本体を保存しない
- スクショ細分類は記録・メモ用途の仮想フォルダであり、写真本体を移動しない
- 一般写真カテゴリは候補表示に留め、OCRだけで断定しない
- 手動分類は自動分類や分類傾向学習より優先する
- 分類傾向学習は端末内の軽量キーワードとメタデータだけを保存する
- 写真本体、サムネイル本体、画像特徴量、大量のOCR全文は学習データに保存しない
- 精度向上モードは初期実装では手動実行を中心にし、1回最大50件だけ処理する
- 「夜間に自動実行を試みる」はiOSの判断に依存し、指定時刻に必ず実行されるものではない
- 精度向上モードでも写真本体は保存、送信、変更しない
- 画像特徴量の本格処理は未実装で、将来検討に留める
- OCR結果削除は `ocr_results.json` と `photo_index.json` の検索データだけを対象にし、写真本体は削除しない
- 分類リセットはしまい箱内の仮想フォルダ情報だけを対象にし、写真アプリ側のアルバムや写真は変更しない
- `photo_index.sqlite` を主ストアとし、旧 `photo_index.json` は移行元/互換バックアップとして残す
- SQLiteでも写真本体やサムネイル本体は保存しない
- 外部APIモードは次段階以降に回す
- App Store提出、証明書、課金関連はこの段階では扱わない
- テスト用の起動引数はDebugビルドのみで使う

## 実機確認メモ

実機へのビルド、インストール、起動は確認済み。以下は端末画面での手動確認として残る。

1. 写真アクセスを未確認から許可へ進める
2. 限定アクセスで数枚だけ選び、グリッドが選択範囲だけになることを確認する
3. 設定画面の「写真の選択を変更」から選択範囲を変更する
4. 1枚OCRして結果が詳細画面に表示されることを確認する
5. アプリを終了して再起動し、OCR結果が残ることを確認する
6. 設定画面のインデックス件数、OCR件数、分類済み件数が復元されることを確認する
7. OCR結果に含まれる語句で検索し、対象写真が表示されることを確認する
8. 1枚のOCR結果を削除し、未処理表示に戻り、同じ語句のOCR検索に出なくなることを確認する
9. 表示中のOCR結果を削除し、検索・カテゴリで絞った範囲だけが対象になることを確認する
10. 全OCR結果キャッシュ削除後、OCR済み件数が0になり、再起動後も削除状態が維持されることを確認する
11. 分類を未分類に戻し、再判定と分類再構築が写真本体を変更せず動くことを確認する
12. カテゴリチップとカテゴリ別件数が自然な速度で表示されることを確認する
13. まとめてOCRを開始し、キャンセル後に完了済みだけ保存されることを確認する
14. 読み込みモードを標準、多め、大量へ切り替え、設定が保存されることを確認する
15. 大量/フルモード選択時に安全確認画面が表示されることを確認する
16. iCloud取得モードを切り替え、端末内未取得写真で失敗表示またはOCR実行を確認する
17. 詳細画面でカテゴリを手動変更し、設定画面の分類傾向学習件数が増えることを確認する
18. 分類傾向学習をオフにして、新しい学習例が増えないことを確認する
19. 学習データを削除し、写真本体、OCR結果、手動分類が残ることを確認する
20. 自動判定に戻した写真で手動分類と学習例が解除されることを確認する
21. 精度向上モードをオンにし、「手動のみ」と「夜間に自動実行を試みる」の表示を確認する
22. 「今すぐ少しだけ実行」を開始し、未分類写真、OCR済み写真、スクショの再判定が進むことを確認する
23. 精度向上モード実行中にキャンセルし、完了件数と中断理由が表示されることを確認する
24. 精度向上データを削除し、分類傾向学習データと処理履歴だけが消えることを確認する

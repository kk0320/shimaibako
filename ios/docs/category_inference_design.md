# 自動分類と仮想フォルダ設計

## 目的

写真本体を動かさず、しまい箱内だけで写真を探しやすく分類する。分類結果は `localIdentifier` に紐づけて保存する。

## カテゴリ

- すべて
- スクショ
- 領収書候補
- 書類写真候補
- 名刺候補
- 看板候補
- ホワイトボード候補
- 工事写真候補
- 旅行写真候補
- 動画
- その他

## 推定方法

OCR前:

- 動画は `mediaType` で分類する
- スクリーンショットは `mediaSubtypes` で分類する
- 書類写真候補は画像の縦横比から低い信頼度で推定する
- 判定できないものはその他にする

OCR後:

- OCRテキスト内の語句からカテゴリを再推定する
- 領収書、名刺、ホワイトボード、工事、看板、旅行、書類の順に候補語句を見る
- 再推定後は `PhotoIndexStore` に保存する

## 信頼度

分類結果は `confidence` を持つ。

- 0.8以上: 高
- 0.5以上: 中
- 0.5未満: 低

詳細画面ではカテゴリ名と信頼度を表示する。

## UI

写真一覧と検索画面にカテゴリチップを表示する。選択したカテゴリで読み込み済み写真を絞り込む。

設定画面にはカテゴリ別件数を表示する。

## 安全方針

- 分類は仮想フォルダであり、写真アプリ側には反映しない
- 写真本体の移動、削除、リネーム、上書きはしない
- カテゴリは再推定できる内部メタデータとして扱う

## 今後の課題

- OCR語句の辞書拡張
- 写真メタデータによる旅行候補の精度改善
- ユーザーによるカテゴリ修正
- SQLiteまたはSwiftDataへの移行時の差分更新

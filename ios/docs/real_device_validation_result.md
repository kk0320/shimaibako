# 実機検証結果

## 検証概要

- 検証日時: 2026-06-17 09:42:11 JST
- 検証端末: K Phone
- 端末種別: iPhone 14 Pro
- 端末モデル: iPhone15,2
- iOSバージョン: 26.5
- Xcodeバージョン: 26.2
- Build version: 17C52
- Bundle Identifier: `com.kk0320.ShimaiBako`
- Signing: Automatic
- Team: `65Q284AMVT`

## ビルド結果

- iOS Simulator build: PASS
- iPhone実機 build: PASS
- warning/error: PASS
- AppIcon構成: PASS
- アプリ表示名: `しまい箱`
- 写真アクセス説明文: `しまい箱が写真を端末内で検索・表示するために使用します。写真は外部送信しません。`

補足:

- Asset catalogからiPad旧バージョン向けアイコンに関するnoticeは出るが、`warning:` と `error:` は出ていない。
- `NSPhotoLibraryUsageDescription` と `PHPhotoLibraryPreventAutomaticLimitedAccessAlert` は設定済み。

## インストール結果

- 実機接続確認: PASS
- 実機インストール: PASS
- 実機起動: PASS

実機は `devicectl` から利用可能な端末として検出され、`com.kk0320.ShimaiBako` をインストールして起動できた。

## 写真アクセス権限ごとの結果

| 状態 | 結果 | メモ |
| --- | --- | --- |
| notDetermined | 手動確認待ち | 初回説明画面とシステム許可ダイアログは端末画面で確認する |
| limited | 手動確認待ち | 「写真を選択」で2から5枚を選び、設定画面の「選択した写真のみ利用中」と「写真の選択を変更」を確認する |
| authorized | 手動確認待ち | すべての写真を許可し、直近100件までのグリッド表示を確認する |
| denied | 手動確認待ち | 設定アプリへの導線が表示され、落ちないことを確認する |
| restricted | 手動確認待ち | 端末や管理設定による制限説明が出ることを確認する |

## OCR結果

- 実機での1枚OCR: 手動確認待ち
- OCR結果表示: 手動確認待ち
- OCR結果保存: 手動確認待ち
- OCR検索: 手動確認待ち

確認手順:

1. 文字入り写真を開く
2. 「この写真をOCR」を実行する
3. 状態、処理日時、OCR結果が詳細画面に表示されることを確認する
4. OCR結果に含まれる語句で検索し、対象写真が出ることを確認する

## まとめてOCR結果

- まとめてOCR: 手動確認待ち
- キャンセル: 手動確認待ち
- 完了済み結果の保持: 手動確認待ち
- 未開始写真が未処理のまま残ること: 手動確認待ち

確認手順:

1. 写真一覧または検索画面で「まとめてOCR」を開始する
2. 対象件数、処理中件数、完了件数、失敗件数を確認する
3. 処理中にキャンセルする
4. キャンセル済み表示と保存済み結果を確認する
5. 再度まとめてOCRを開始できることを確認する

## 再起動後復元結果

- アプリ再起動後のOCR結果復元: 手動確認待ち

確認手順:

1. OCRを1件以上完了する
2. アプリを終了する
3. アプリを再起動する
4. 詳細画面にOCR結果が残っていることを確認する
5. OCR語句で検索できることを確認する

## 見つかった不具合

- 実機ビルド、インストール、起動の範囲では致命的な不具合なし。
- Simulator画面で、サムネイル上のOCR状態バッジが狭いセルで切れやすかったため、グリッド用バッジ文言を短く調整した。

## 次の対応

1. 実機上で写真アクセスの limited / authorized / denied を手動確認する
2. 実機写真で1枚OCR、まとめてOCR、キャンセルを確認する
3. アプリ再起動後のOCR結果復元を確認する
4. OCR検索が実機写真で動くことを確認する
5. 写真が削除、移動、リネーム、上書きされていないことを目視確認する
6. 必要に応じて通信監視で外部送信がないことを確認する

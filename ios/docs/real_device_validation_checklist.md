# 実機確認チェックリスト

## 目的

実際の写真ライブラリ、限定アクセス、端末内OCR、再起動後のOCR結果復元が安全に動くことを確認する。

## 対象端末

- iPhone実機
- 最新のiOS
- 写真が数十枚以上ある状態
- スクリーンショットや文字入り写真が数枚ある状態

## 今回の実行結果

検証日時: 2026-06-17 09:42:11 JST

検証環境:

- Mac側: Xcode 26.2
- 接続端末: K Phone
- 端末種別: iPhone 14 Pro
- iOS: 26.5
- Bundle Identifier: `com.kk0320.ShimaiBako`
- Signing: Automatic

自動確認できた項目:

- `git status`: 作業前はclean、作業後はこの検証docs更新と軽微なUI調整のみ
- `xcodebuild build` for iOS Simulator: PASS
- `xcodebuild build` for iPhone実機: PASS
- build warning/error: PASS
- 実機インストール: PASS
- 実機起動: PASS
- Simulator起動: PASS
- `xcuserdata` と `UserInterfaceState` がGit管理対象に入っていない: PASS
- 禁止表現チェック: PASS

端末上の手動操作が必要なため、今回の自動実行だけでは未完了の項目:

- 写真アクセス limited の選択と表示確認
- 写真アクセス denied の表示確認
- 写真アクセス authorized の表示確認
- 実機写真での写真グリッド確認
- 実機写真での詳細表示確認
- 実機写真での1枚OCR確認
- 実機写真でのまとめてOCR確認
- まとめてOCRキャンセル確認
- OCR検索確認
- アプリ再起動後のOCR結果復元確認
- 写真が削除・変更されていないことの目視確認
- 外部通信を使わないことの通信監視確認

## ビルド前確認

- [x] `git status` に意図しない変更がない
- [x] `xcuserdata` と `UserInterfaceState` がGit管理対象に入っていない
- [x] `xcodebuild build` がwarning/errorなしで成功する
- [x] 外部APIキーや外部サービス設定がない

## 写真アクセス許可

状態: 実機インストールと起動はPASS。許可ダイアログ操作は手動確認待ち。

1. アプリを削除して再インストールする
2. 初回起動で写真アクセス前の説明が表示される
3. 「写真へのアクセスを許可」からシステム許可へ進む
4. すべての写真を許可した場合、直近100件まで表示される

## limited選択確認

状態: 手動確認待ち。

1. アプリを削除して再インストールする
2. 写真アクセスで「写真を選択」を選ぶ
3. 2から5枚程度だけ選ぶ
4. 写真タブに選択した写真だけが表示される
5. 設定画面に「選択した写真のみ利用中」と表示される
6. 「写真の選択を変更」から選択範囲を変更できる
7. 変更後の範囲でグリッドが更新される

## 写真グリッド確認

状態: Simulator起動では表示PASS。実機写真での表示は手動確認待ち。

- サムネイルが崩れず表示される
- 写真、スクリーンショット、動画のフィルタが動く
- OCR状態バッジが表示される
- 下部タブと重ならない

## 詳細表示確認

状態: 実機写真での表示は手動確認待ち。

- 写真をタップして詳細画面が開く
- 撮影日、種類、サイズ、識別子が表示される
- 画像取得に失敗してもアプリが落ちない

## 1枚OCR確認

状態: 実機写真では手動確認待ち。

- 文字入り写真で「この写真をOCR」を実行する
- OCR中の状態が表示される
- OCR済み状態、処理日時、OCR結果が表示される
- OCR済み写真では「再OCR」が表示される

## まとめてOCR確認

状態: 実機写真では手動確認待ち。

- 写真一覧または検索画面で「まとめてOCR」を実行する
- 最大20件までが対象になる
- 対象件数、処理中件数、完了件数、失敗件数が表示される
- 処理中にUIが固まらない

## キャンセル確認

状態: 実機写真では手動確認待ち。

- まとめてOCR中にキャンセルする
- キャンセル済み状態が表示される
- 完了済みのOCR結果は残る
- 未開始の写真は未処理のまま残る
- もう一度まとめてOCRを開始できる

## OCR検索確認

状態: 実機写真では手動確認待ち。

- OCR結果に含まれる語句で検索する
- 対象写真が検索結果に表示される
- OCR未処理写真はOCRテキスト検索に含まれない

## 再起動後のOCR結果復元確認

状態: 実機写真では手動確認待ち。

- OCR実行後にアプリを終了する
- アプリを再起動する
- 詳細画面にOCR結果が残っている
- OCR語句で検索できる

## 設定画面確認

状態: Simulator表示はPASS。実機でのlimited/denied表示は手動確認待ち。

- 写真アクセス状態が表示される
- 読み込み上限が表示される
- OCR済み件数、未処理件数、失敗件数が表示される
- OCR言語、精度、まとめてOCR上限、画像サイズ目安が表示される
- 外部送信、保存先、写真操作の方針が表示される
- 安全方針の順番が維持されている

## 写真操作確認

状態: 実機写真では手動確認待ち。

- 写真が削除されていない
- 写真が移動されていない
- 写真がリネームされていない
- 写真本体が上書きされていない

## 外部通信確認

状態: コード上は外部送信処理なし。通信監視は手動確認待ち。

- 写真本体を外部送信しない
- 外部OCRサービスを使わない
- APIキーなしで動作する

## 注意点

- OCR結果キャッシュは写真本体ではなく、認識したテキストと状態だけを保存する
- limited選択の変更はiOS標準画面で確認する
- iCloud上にあり端末内で取得できない写真は、失敗表示になってもアプリが落ちなければよい
